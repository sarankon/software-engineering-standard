Implemented Project Onboarding orchestration. Added `ai_development/onboarding/project-onboarding.md` as the umbrella procedure for new, existing, and update flows; added `schemas/project-onboarding/README.md` for point-in-time onboarding run traceability; added `examples/project-onboarding/onboarding.yaml`. The procedure explicitly uses source/configuration/project-owned docs as evidence, forbids Git history/messages/diffs/branches/tags/blame as onboarding evidence, preserves confirmed decisions, requires explicit user approval before project changes, and requires idempotent/update-safe reruns. Existing repositories continue to use `blueprints/project_context/ONBOARDING.md`; new projects use `ai_development/bootstrap/installation-procedure.md`. Updated `AI_BOOTSTRAP.md`, `README.md`, and `ai_development/README.md` to expose the onboarding flow and corrected the new-project procedure relationship. Validation: `git diff --check` passed; existing onboarding fixture has no working-tree diff. Implementation is committed after this memory update.