S3 implementation direction: proceed from the consolidated canonical `standards/SPECIFICATION.md` to the first concrete Software Standard. Start with the Security Standard because it is foundational and cross-cutting, then use the same rule model for Database/UI/etc. Preserve existing standards and avoid silently replacing prior rules. Concrete rules use stable SES Rule IDs and the original five normative levels MUST/MUST NOT/SHOULD/SHOULD NOT/MAY. Standard governance separates record_state from lifecycle/status. AI reads/evaluates/proposes; user remains the approval boundary for project changes. Before implementation, this direction is recorded and committed per project workflow.