Architecture decision direction approved by user on 2026-09-03: Project AI interaction should have a single project-level entry point at `AGENTS.md`. Framework installation should establish a durable navigation chain rather than requiring the AI to rediscover Framework ad hoc on every session. Canonical target layout is `AGENTS.md` → `.project-context/AI_INSTRUCTIONS.md` → `.project-context/project-standard/` containing installed Framework artifacts such as `blueprints/`, `procedures/`, `standards/`, and `manifest.yaml`, alongside project-specific context domains under `.project-context/`. `.project-context/` is the project-owned home for both Project Context and installed Project Standard artifacts. Do not create a nested `.project-context/project-standard/project-context/` structure. `AGENTS.md` is the single project AI entry point; future vendor-specific files such as `CLAUDE.md` or similar should, where supported, point back to `AGENTS.md` rather than duplicating Framework instructions. Existing `AGENTS.md` content must not be overwritten; Framework installation/update should use an additive, traceable integration approach. This decision is an architecture direction to be formalized in Framework contracts before implementation. User approved proceeding with this direction; next work is to formalize the architecture decision, then update Installation/Bootstrap contracts, Update/Migration behavior, Existing Repository Onboarding, and tests.