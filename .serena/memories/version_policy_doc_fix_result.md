Version policy documentation fix completed on 2026-09-03. Updated `standards/versioning.md` example so framework-owned Project Context schema is `1.0.0` and Software Standard `SES-DB` is `0.1.0`, matching the approved pre-production policy. No historical memories or actual release/version metadata were changed. `git diff --check` passes. Framework-owned components remain 1.0.0; Software Standards remain 0.1.0 until the user explicitly approves a version change.