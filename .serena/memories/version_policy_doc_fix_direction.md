Next implementation direction, 2026-09-03: Apply the latest approved pre-production version policy to the concrete documentation example in `standards/versioning.md`. The example currently conflicts with policy by showing Project Context schema `0.1.0` and `SES-DB` standard `1.0.0`; update it to framework-owned schema `1.0.0` and Software Standard `0.1.0`. This is a documentation-only consistency fix; do not change historical memories or actual release/version metadata. Framework-owned components remain 1.0.0 and Software Standards remain 0.1.0 until the user explicitly approves a version change.