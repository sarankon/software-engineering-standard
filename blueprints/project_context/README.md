# Project Context Blueprint

Blueprint for initializing and maintaining a project's `project_context/`.

The Blueprint defines structure, not project-specific knowledge. A project receives an instance of this Blueprint; it must not be overwritten wholesale when the Blueprint changes.

Planned areas:

- `development_standard/`
- `ai_development/`
- `knowledge_base/`
- `decisions/`
- `state/`
